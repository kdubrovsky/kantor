// Constant objects

// Can you change an object that created with const?
// https://learn.javascript.ru/task/const-object

const user = {
    name: "John"
};

// does it work?
user.name = "Pete";
console.log(user);

  // Yes we can change const objects. It works.